/**
 * The app's name and its payoff, in one place.
 *
 * Four surfaces need to agree on the exact string — the page title, the PWA manifest,
 * the header brand, and the public page — and a rename typed four times is a rename
 * that drifts the first time only three of the four are found.
 */
export const APP_NAME = 'Songbook'

/** Deliberately English, and deliberately not translated on an Italian page: a payoff is heard, not read for meaning. */
export const APP_PAYOFF = 'Where every fire needs a melody'
